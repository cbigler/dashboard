import { DensitySpace } from '../../types';

export default function analyticsColorScale(space: DensitySpace) {
  // FIXME: Colors are not yet defined, please point this out in a review!
  return '#ff0000';
}
