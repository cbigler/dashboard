
import assert from 'assert';
import spaceHierarchySearcher from './index';

describe('space-hierarchy-searcher', function() {
  it('should work', function() {
    assert.equal(spaceHierarchySearcher(), true);
  });
});

