import assert from 'assert';
import spaceHierarchyFormatter from './index';

describe('space-hierarchy-formatter', function() {
  it('should work', function() {
    assert.equal(spaceHierarchyFormatter(), true);
  });
});

